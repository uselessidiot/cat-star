# Supabase 프로젝트 Settings > API 에서 값을 복사해 .env.local 에 붙여넣으세요.
# anon key는 공개용으로 설계된 키라 커밋해도 안전하지만(RLS가 보호), 여기서는 파일 자체를 .env.local로 분리해 관리합니다.
# service_role 키는 이 프로젝트 어디에도 필요하지 않습니다 — 절대 추가하지 마세요.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
